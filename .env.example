# This example .env was used by an earlier temporary AI editor feature and
# has been cleared to avoid exposing keys. If you need to configure the
# backend API base URL for the Flutter app, create a `.env` file with:
# API_BASE_URL=http://your-backend:5000
